# this is a comment
def foo(x)
{
    x = true;
    emit x;
}

main(y)
{
    add(123, 456.0);
    f = foo(y);
    y = and(f, false);
    z = foo(y);
    g = and(y,z);
    a = select(1,g);
    emit a;
}