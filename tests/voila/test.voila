# this is a comment
def foo(x)
{
    x = true;
    emit x;
}

main(y)
{
    add(123, 456.0);
    f = foo(y);
    y = and(f, false); #TODO: y is tensor and not lowered
    select(42, y);
    z = foo(y);
    emit and(y,z);
}