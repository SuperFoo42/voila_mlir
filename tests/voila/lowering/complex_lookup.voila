// RUN: voila-mlir-translate -emit=mlir %s INT32:100 INT32:100 | FileCheck %s

// CHECK-LABEL: func @main
main(x,y)
{
// CHECK-NEXT: voila.hash
    h = hash(x,y);
    a,b = insert(h,x,y);
    z = lookup(x, y, a, b, h);
    emit z;
}